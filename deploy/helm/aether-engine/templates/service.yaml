apiVersion: v1
kind: Service
metadata:
  name: {{ include "aether-engine.fullname" . }}
  labels:
    {{- include "aether-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aether-engine.selectorLabels" . | nindent 4 }}
